# google-ads-budget-alerts
This Google Ads Script is designed to run in a Manager Account (MCC) and check yesterday’s total spend per client account. It automatically compiles a list of accounts whose spend falls below a configurable threshold and sends a single summary email alert.
